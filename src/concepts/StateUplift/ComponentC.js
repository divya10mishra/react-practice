import React from 'react'

function ComponentC({name,email,phone}) {
  return (
    <div>
        <div>name:{name}</div>
        <div>email:{email}</div>
        <div>phone:{phone}</div>
    </div>
  )
}

export default ComponentC